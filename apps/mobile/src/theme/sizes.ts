import type { ThemeSizes } from './types';

export const sizes: ThemeSizes = {
  icon: {
    sm: 16,
    md: 20,
    lg: 24,
    xl: 28,
  },
  touch: {
    /** Apple HIG minimum touch target */
    min: 44,
  },
};
