/**
 * Forced charcoal palette kept as a thin re-export for any leftover imports.
 * New code should use getLoginColors(colorScheme) from ./loginColors.
 */
export { authColors, getLoginColors, type LoginColors } from './loginColors';
