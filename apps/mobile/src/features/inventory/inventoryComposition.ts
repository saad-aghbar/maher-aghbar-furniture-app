/**
 * Inventory tab composition switch.
 *
 * - `signature` — category rail + low-stock focus + searchable materials (default)
 * - `classic` — original groups hub FlatList
 */
export type InventoryComposition = 'signature' | 'classic';

export const INVENTORY_COMPOSITION: InventoryComposition = 'signature';
