/** FAB destination — basket first, then confirm opens create-order. */
export const DEALER_NEW_ORDER_HREF = '/(app)/(customer)/(tabs)/basket' as const;

/** i18n key for FAB accessibility label (RTL-safe via t()). */
export const DEALER_NEW_ORDER_A11Y_KEY = 'mobile.tabs.newOrder' as const;
