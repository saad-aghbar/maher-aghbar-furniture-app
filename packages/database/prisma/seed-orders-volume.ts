/**
 * @deprecated Volume orders are now generated inside seed/sales-timeline.ts
 * as part of the coherent 8-month world. Kept as a no-op export for safety.
 */
export async function seedOrdersVolume(): Promise<number> {
  return 0;
}

export async function seedNewOrderRfqs(): Promise<number> {
  return 0;
}
