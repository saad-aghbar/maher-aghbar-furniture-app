# Unified web overhaul evidence

Walk the live app at `http://localhost:3000`.

Demo logins (password `123`):

- Admin: `admin` → `/ar/admin/dashboard`
- Dealer: `nile` → `/ar/dealer/dashboard`
- Worker: `carpenter` / `driver` → `/ar/worker/dashboard`
- Staff: `warehouse` → `/ar/admin/dashboard` (warehouse home)

Playwright: `e2e/web-parity-walk.spec.ts`.
